# 🎮 ROKhub - منصة الألعاب الرقمية

<div align="center">

![ROKhub Logo](static/images/og-image.png)

[![Flask](https://img.shields.io/badge/Flask-3.0.0-blue)](https://flask.palletsprojects.com/)
[![Python](https://img.shields.io/badge/Python-3.8+-green)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-yellow)](LICENSE)

**أفضل منصة ألعاب رقمية عربية متكاملة**

[الرئيسية](#) • [المميزات](#المميزات) • [التثبيت](#التثبيت) • [الاستخدام](#الاستخدام)

</div>

---

## 📋 المميزات

### 🎮 إدارة الألعاب
- ✅ إضافة وتعديل وحذف الألعاب
- ✅ تصنيف الألعاب حسب الفئات
- ✅ دعم ألعاب PC والموبايل
- ✅ تتبع عدد التحميلات والمشاهدات
- ✅ فحص صحة روابط التحميل

### 👥 نظام المستخدمين
- ✅ تسجيل دخول وتسجيل مستخدمين جدد
- ✅ نظام أدوار (Admin/User)
- ✅ حماية من محاولات الدخول الفاشلة
- ✅ ملفات شخصية للمستخدمين
- ✅ نظام رسائل ومحادثات

### 🛡️ الأمان
- ✅ حماية CSRF
- ✅ Rate Limiting
- ✅ Content Security Policy
- ✅ تشفير كلمات المرور
- ✅ جلسات آمنة

### 🎨 الواجهة
- ✅ تصميم عصري ومتجاوب
- ✅ دعم اللغة العربية (RTL)
- ✅ وضع مظلم/فاتح
- ✅ إشعارات فورية
- ✅ رسوم متحركة سلسة

### 🔧 الأدوات
- ✅ لوحة تحكم Admin متكاملة
- ✅ نظام إصلاح الأخطاء التلقائي
- ✅ سجل أخطاء مفصل
- ✅ نظام إشعارات
- ✅ دعم متعدد اللغات

---

## 🚀 التثبيت

### المتطلبات
- Python 3.8 أو أحدث
- pip
- virtualenv (موصى به)

### خطوات التثبيت

1. **استنساخ المستودع**
```bash
git clone https://github.com/roknoroknohh/rokno.git
cd gamebad
```

2. **إنشاء بيئة افتراضية**
```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **تثبيت المتطلبات**
```bash
pip install -r requirements.txt
```

4. **إعداد الإعدادات**
```bash
cp .env.example .env
# عدل ملف .env حسب إعداداتك
```

5. **تهيئة قاعدة البيانات**
```bash
python -c "from app import app; from utils import init_database; app.app_context().push(); init_database()"
# أو افتح المتصفح واذهب إلى: http://localhost:5000/init
```

6. **تشغيل التطبيق**
```bash
python app.py
```

7. **افتح المتصفح**
```
http://localhost:5000
```

---

## 🔑 بيانات الدخول الافتراضية

| الدور | اسم المستخدم | كلمة المرور |
|-------|-------------|------------|
| Admin | admin | (القيمة في config.py) |

---

## 📁 هيكل المشروع

```
gamebad/
├── app.py                 # التطبيق الرئيسي
├── models.py              # نماذج قاعدة البيانات
├── utils.py               # الأدوات المساعدة
├── config.py              # الإعدادات
├── requirements.txt       # المتطلبات
├── static/                # الملفات الثابتة
│   ├── css/
│   ├── js/
│   └── images/
├── templates/             # القوالب
│   ├── admin/
│   ├── user/
│   └── errors/
├── instance/              # قاعدة البيانات
└── logs/                  # سجلات الأخطاء
```

---

## 🔧 الإعدادات

### متغيرات البيئة

| المتغير | الوصف | القيمة الافتراضية |
|---------|------|------------------|
| `FLASK_ENV` | بيئة التشغيل | `development` |
| `SECRET_KEY` | المفتاح السري | (مطلوب) |
| `DATABASE_URL` | قاعدة البيانات | `sqlite:///rokhub.db` |
| `ADMIN_PASSWORD` | كلمة مرور الأدمن | (مطلوب) |

---

## 🛠️ التطوير

### إضافة لعبة جديدة
```python
from app import app
from models import db, Game, Category

with app.app_context():
    game = Game(
        title='اسم اللعبة',
        description='وصف اللعبة',
        platform='both',  # pc, mobile, both
        download_url='https://example.com/download',
        external_url='https://example.com',
        cover_image='https://example.com/image.jpg',
        category_id=1
    )
    db.session.add(game)
    db.session.commit()
```

### إضافة فئة جديدة
```python
category = Category(
    name='اسم الفئة',
    icon='gamepad',
    color='#3b82f6'
)
```

---

## 🐛 حل المشاكل

### مشكلة: لا يمكن تسجيل الدخول
- تأكد من تشغيل `/init` أولاً
- تحقق من كلمة المرور في `config.py`

### مشكلة: الصور لا تظهر
- تأكد من وجود مجلد `static/images/`
- تحقق من صلاحيات المجلد

### مشكلة: قاعدة البيانات
```bash
# حذف وإعادة إنشاء
rm instance/rokhub.db
python -c "from app import app; app.app_context().push(); from utils import init_database; init_database()"
```

---

## 🤝 المساهمة

نرحب بمساهماتكم! يرجى اتباع الخطوات:

1. Fork المشروع
2. إنشاء فرع جديد (`git checkout -b feature/amazing`)
3. Commit التغييرات (`git commit -m 'Add amazing'`)
4. Push للفرع (`git push origin feature/amazing`)
5. فتح Pull Request

---

## 📜 الترخيص

هذا المشروع مرخص بموجب [MIT License](LICENSE)

---

## 👨‍💻 المطور

**ROKhub Team** 

- Instagram: [@rokno_rokno](https://www.instagram.com/rokno_rokno)

---

<div align="center">

**صنع بـ ❤️ في الوطن العربي**

</div>
