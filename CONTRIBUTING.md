# دليل المساهمة في ROKhub

شكراً لاهتمامك بالمساهمة في ROKhub! 🎮

## كيفية المساهمة

### 1. الإبلاغ عن الأخطاء (Bug Reports)

عند الإبلاغ عن خطأ، يرجى تضمين:
- وصف واضح للمشكلة
- خطوات إعادة إنتاج الخطأ
- الصور أو الفيديوهات (إن أمكن)
- معلومات النظام (OS, Browser, etc.)

### 2. اقتراح ميزات جديدة

- افتح Issue جديد مع وسم `enhancement`
- وصف الميزة المقترحة
- توضيح الفائدة المتوقعة

### 3. Pull Requests

#### خطوات المساهمة بالكود:

1. **Fork المشروع**
   ```bash
   git clone https://github.com/your-username/rokno.git
   cd gamebad
   ```

2. **إنشاء فرع جديد**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **إجراء التغييرات**
   - اكتب كود نظيف ومعلق
   - اتبع معايير PEP 8
   - أضف اختبارات إن أمكن

4. **Commit**
   ```bash
   git add .
   git commit -m "feat: add new feature description"
   ```

5. **Push**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **افتح Pull Request**

### معايير الكود

#### Python
- اتبع PEP 8
- استخدم docstrings للدوال
- أضف type hints عند الإمكان
- اكتب اختبارات للدوال الجديدة

```python
def calculate_discount(price: float, discount: float) -> float:
    """
    حساب السعر بعد الخصم.
    
    Args:
        price: السعر الأصلي
        discount: نسبة الخصم (0-1)
    
    Returns:
        السعر بعد الخصم
    """
    return price * (1 - discount)
```

#### HTML/CSS
- استخدم Tailwind CSS
- تأكد من التصميم المتجاوب
- دعم اللغة العربية (RTL)

#### JavaScript
- استخدم ES6+
- أضف تعليقات للكود المعقد
- تجنب jQuery (استخدم Vanilla JS)

### اختبارات

قبل إرسال PR:

```bash
# تشغيل الاختبارات
make test

# أو
pytest tests/ -v
```

### أنماط Commit Messages

استخدم نمط Conventional Commits:

- `feat:` ميزة جديدة
- `fix:` إصلاح خطأ
- `docs:` تغييرات في التوثيق
- `style:` تغييرات في التنسيق
- `refactor:` إعادة هيكلة الكود
- `test:` إضافة/تعديل اختبارات
- `chore:` مهام صيانة

### قائمة المهام قبل PR

- [ ] الكود يعمل بشكل صحيح
- [ ] تمت إضافة/تحديث الاختبارات
- [ ] التوثيق محدث
- [ ] لا توجد أخطاء في pylint
- [ ] تم اختبار التصميم المتجاوب

## الأسئلة؟

لأي استفسار، افتح Issue أو تواصل معنا على:
- Instagram: [@rokno_rokno](https://www.instagram.com/rokno_rokno)

---

**شكراً لمساهمتك! 🙏**
